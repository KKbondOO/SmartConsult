# ==================== 核心框架 ====================
# Gradio UI 框架
gradio>=4.0.0

# LangChain 和 LangGraph
langchain>=0.1.0
langchain-core>=0.1.0
langchain-openai>=0.0.5
langgraph>=0.0.20
langchain-mcp-adapters>=0.1.0

# ==================== 环境配置 ====================
# 环境变量管理
python-dotenv>=1.0.0

# 数据验证
pydantic>=2.0.0

# ==================== AI 模型 ====================
# OpenAI 兼容接口
openai>=1.0.0

# Transformers (用于 Whisper 语音识别)
transformers>=4.35.0
accelerate>=0.25.0

# ==================== 语音处理 ====================
# TTS 语音合成 (Kokoro)
kokoro-onnx>=0.2.0
# 如果使用 PyTorch 版本的 Kokoro，取消下面的注释
# kokoro>=0.1.0

# PyTorch (用于语音模型)
torch>=2.0.0
torchaudio>=2.0.0

# 音频处理
soundfile>=0.12.0
numpy>=1.24.0

# ==================== 文本处理 ====================
# 繁简转换
opencc-python-reimplemented>=0.1.7

# ==================== 其他依赖 ====================
# HTTP 请求
requests>=2.31.0
httpx>=0.25.0

# 异步支持
aiohttp>=3.9.0
asyncio

# JSON 处理
orjson>=3.9.0

kokoro